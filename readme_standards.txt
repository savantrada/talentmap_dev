
FOR PHP:
	*Always declare any static values in config.php file (config/congif.php)
	*Always use require_once '<path to php file>' in starting of any php file.
	*There should be only one echo statment at the end i.e is in webapi.php file, nowhere else.	